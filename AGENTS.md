This repository is for the development of the OpenCode mobile application.
Any questions you get are always related to the mobile application.

The server and other clients live in the same repository as a reference.

You are only allowed to edit files in `packages/mobile`.
Everything else is read-only and should be considered as a reference implementation.

See `packages/mobile/AGENTS.md` for further information.
